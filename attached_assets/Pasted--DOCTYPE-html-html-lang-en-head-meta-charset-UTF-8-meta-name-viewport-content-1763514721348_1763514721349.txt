<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Debugging in Space - Game Mockup</title>
    <script crossorigin src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', 'Cantarell', sans-serif;
        }
        @keyframes fade-in {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        .animate-fade-in {
            animation: fade-in 0.3s ease-in;
        }
    </style>
</head>
<body>
    <div id="root"></div>

    <script type="text/babel">
        const { useState, useEffect } = React;

        // Lucide React icons as inline SVG components
        const Book = ({ size = 24, className = "" }) => (
            <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className={className}>
                <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"></path>
                <path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"></path>
            </svg>
        );

        const FileText = ({ size = 24, className = "" }) => (
            <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className={className}>
                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
                <polyline points="14 2 14 8 20 8"></polyline>
                <line x1="16" y1="13" x2="8" y2="13"></line>
                <line x1="16" y1="17" x2="8" y2="17"></line>
                <polyline points="10 9 9 9 8 9"></polyline>
            </svg>
        );

        const Activity = ({ size = 24, className = "" }) => (
            <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className={className}>
                <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"></polyline>
            </svg>
        );

        const AlertTriangle = ({ size = 24, className = "" }) => (
            <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className={className}>
                <path d="m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3Z"></path>
                <line x1="12" y1="9" x2="12" y2="13"></line>
                <line x1="12" y1="17" x2="12.01" y2="17"></line>
            </svg>
        );

        const Zap = ({ size = 24, className = "" }) => (
            <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className={className}>
                <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"></polygon>
            </svg>
        );

        const Wind = ({ size = 24, className = "" }) => (
            <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className={className}>
                <path d="M9.59 4.59A2 2 0 1 1 11 8H2m10.59 11.41A2 2 0 1 0 14 16H2m15.73-8.27A2.5 2.5 0 1 1 19.5 12H2"></path>
            </svg>
        );

        const Navigation = ({ size = 24, className = "" }) => (
            <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className={className}>
                <polygon points="3 11 22 2 13 21 11 13 3 11"></polygon>
            </svg>
        );

        function SpaceStationGame() {
            const [screen, setScreen] = useState('menu');

            return (
                <div className="w-full h-screen bg-gray-900 flex items-center justify-center p-4">
                    {screen === 'menu' ? (
                        <MainMenu onStart={() => setScreen('game')} />
                    ) : (
                        <GameWorld onMenu={() => setScreen('menu')} />
                    )}
                </div>
            );
        }

        function MainMenu({ onStart }) {
            return (
                <div className="relative w-full max-w-4xl h-[600px] bg-gradient-to-b from-indigo-950 via-purple-950 to-black rounded-lg border-2 border-cyan-500 shadow-2xl overflow-hidden">
                    <div className="absolute inset-0 opacity-30">
                        {[...Array(50)].map((_, i) => (
                            <div
                                key={i}
                                className="absolute w-1 h-1 bg-white rounded-full animate-pulse"
                                style={{
                                    left: `${Math.random() * 100}%`,
                                    top: `${Math.random() * 100}%`,
                                    animationDelay: `${Math.random() * 3}s`,
                                }}
                            />
                        ))}
                    </div>

                    <div className="relative z-10 flex flex-col items-center justify-center h-full space-y-8">
                        <div className="text-center space-y-4">
                            <h1 className="text-6xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 via-blue-400 to-purple-400 tracking-wider animate-pulse">
                                DEBUGGING IN SPACE
                            </h1>
                            <p className="text-cyan-300 text-xl tracking-widest">STATION AURORA</p>
                            <div className="flex items-center justify-center gap-2 text-yellow-400">
                                <AlertTriangle size={20} />
                                <p className="text-sm">CRITICAL SYSTEMS FAILURE</p>
                                <AlertTriangle size={20} />
                            </div>
                        </div>

                        <div className="flex flex-col gap-4 w-64">
                            <button
                                onClick={onStart}
                                className="bg-gradient-to-r from-cyan-600 to-blue-600 hover:from-cyan-500 hover:to-blue-500 text-white font-bold py-4 px-8 rounded-lg border-2 border-cyan-400 shadow-lg hover:shadow-cyan-500/50 transition-all transform hover:scale-105"
                            >
                                NEW GAME
                            </button>
                            <button className="bg-gray-700 hover:bg-gray-600 text-gray-300 font-bold py-4 px-8 rounded-lg border-2 border-gray-500 shadow-lg transition-all">
                                CONTINUE
                            </button>
                            <button className="bg-gray-700 hover:bg-gray-600 text-gray-300 font-bold py-4 px-8 rounded-lg border-2 border-gray-500 shadow-lg transition-all">
                                SETTINGS
                            </button>
                        </div>

                        <p className="text-gray-500 text-sm absolute bottom-4">
                            CS309 | Johnston Liu, Mercy Omwoyo, Sarang Joshi
                        </p>
                    </div>
                </div>
            );
        }

        function GameWorld({ onMenu }) {
            const [playerPos, setPlayerPos] = useState({ x: 400, y: 350 });
            const [activeTab, setActiveTab] = useState('log');
            const [nearbyObject, setNearbyObject] = useState(null);
            const [inspecting, setInspecting] = useState(null);

            const objects = [
                { id: 1, x: 180, y: 200, type: 'panel', icon: '⚡', label: 'Circuit Breaker', status: 'critical', color: 'red' },
                { id: 2, x: 450, y: 180, type: 'switch', icon: '🔌', label: 'Power Switch A7', status: 'warning', color: 'yellow' },
                { id: 3, x: 650, y: 240, type: 'terminal', icon: '💻', label: 'Diagnostic Terminal', status: 'active', color: 'green' },
                { id: 4, x: 300, y: 450, type: 'vent', icon: '🌀', label: 'Air Vent Control', status: 'warning', color: 'yellow' },
                { id: 5, x: 550, y: 400, type: 'locker', icon: '📦', label: 'Equipment Storage', status: 'inactive', color: 'gray' },
            ];

            const doors = [
                { id: 'd1', x: 380, y: 120, label: 'To Life Support', locked: true },
                { id: 'd2', x: 100, y: 300, label: 'To Crew Quarters', locked: false },
            ];

            const handleMapClick = (e) => {
                const rect = e.currentTarget.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                setPlayerPos({ x, y });
                setInspecting(null);
            };

            useEffect(() => {
                let closest = null;
                let minDist = Infinity;

                objects.forEach(obj => {
                    const dist = Math.sqrt(Math.pow(playerPos.x - obj.x, 2) + Math.pow(playerPos.y - obj.y, 2));
                    if (dist < 80 && dist < minDist) {
                        minDist = dist;
                        closest = obj;
                    }
                });

                setNearbyObject(closest);
            }, [playerPos]);

            return (
                <div className="w-full max-w-7xl h-[800px] bg-gray-900 rounded-lg border-2 border-cyan-600 shadow-2xl overflow-hidden flex flex-col">
                    {/* Top HUD Bar */}
                    <div className="bg-gray-800 border-b-2 border-cyan-600 p-3 flex items-center justify-between">
                        <button
                            onClick={onMenu}
                            className="text-cyan-400 hover:text-cyan-300 text-sm font-bold"
                        >
                            ← MAIN MENU
                        </button>
                        
                        <div className="text-cyan-300 text-lg font-bold">ENGINEERING BAY - LEVEL 1</div>
                        
                        <div className="flex gap-4">
                            <div className="flex items-center gap-2">
                                <span className="text-green-400 text-sm font-bold">HEALTH</span>
                                <div className="w-32 h-5 bg-gray-700 rounded border border-green-400">
                                    <div className="h-full bg-gradient-to-r from-green-500 to-green-400 rounded" style={{ width: '85%' }}></div>
                                </div>
                            </div>
                            
                            <div className="flex items-center gap-2">
                                <span className="text-cyan-400 text-sm font-bold">O₂</span>
                                <div className="w-32 h-5 bg-gray-700 rounded border border-cyan-400">
                                    <div className="h-full bg-gradient-to-r from-cyan-500 to-blue-400 rounded animate-pulse" style={{ width: '62%' }}></div>
                                </div>
                            </div>
                        </div>
                    </div>

                    {/* Main Game Area */}
                    <div className="flex-1 flex overflow-hidden">
                        {/* Left Panel - Objectives */}
                        <div className="w-56 bg-gray-800 border-r-2 border-cyan-600 p-3 overflow-y-auto">
                            <h3 className="text-cyan-400 font-bold text-sm mb-3 flex items-center gap-2">
                                <Activity size={16} />
                                OBJECTIVES
                            </h3>
                            
                            <div className="space-y-2 text-xs">
                                <div className="p-2 bg-yellow-900/30 border-l-4 border-yellow-400 rounded">
                                    <div className="flex items-center gap-2 text-yellow-200">
                                        <AlertTriangle size={14} className="animate-pulse" />
                                        <span className="font-bold">ACTIVE</span>
                                    </div>
                                    <p className="mt-1 text-gray-300">Restore main power to Engineering Bay</p>
                                </div>
                                
                                <div className="p-2 bg-gray-700/50 border-l-4 border-gray-500 rounded text-gray-400">
                                    <p>Fix oxygen circulation systems</p>
                                </div>
                                
                                <div className="p-2 bg-gray-700/50 border-l-4 border-gray-500 rounded text-gray-400">
                                    <p>Repair navigation controls</p>
                                </div>
                            </div>

                            <div className="mt-4 pt-4 border-t border-gray-700">
                                <h4 className="text-cyan-400 font-bold text-xs mb-2">SYSTEMS STATUS</h4>
                                <div className="space-y-1 text-xs">
                                    <div className="flex items-center gap-2">
                                        <Zap size={12} className="text-red-400" />
                                        <span className="text-red-400">Power: CRITICAL</span>
                                    </div>
                                    <div className="flex items-center gap-2">
                                        <Wind size={12} className="text-yellow-400" />
                                        <span className="text-yellow-400">Life Support: WARNING</span>
                                    </div>
                                    <div className="flex items-center gap-2">
                                        <Navigation size={12} className="text-red-400" />
                                        <span className="text-red-400">Navigation: ERROR</span>
                                    </div>
                                </div>
                            </div>

                            <div className="mt-4 p-2 bg-cyan-900/20 border border-cyan-600 rounded text-xs">
                                <p className="text-cyan-300 font-bold mb-1">💡 TIP</p>
                                <p className="text-gray-300">Click anywhere to move. Get close to objects to interact with them.</p>
                            </div>
                        </div>

                        {/* Center - Game World */}
                        <div className="flex-1 relative bg-gray-800">
                            <div 
                                className="absolute inset-0 cursor-crosshair overflow-hidden"
                                onClick={handleMapClick}
                                style={{
                                    backgroundImage: 'linear-gradient(rgba(0, 200, 200, 0.03) 1px, transparent 1px), linear-gradient(90deg, rgba(0, 200, 200, 0.03) 1px, transparent 1px)',
                                    backgroundSize: '30px 30px'
                                }}
                            >
                                {/* Station walls and structure */}
                                <div className="absolute inset-4 border-4 border-gray-700 rounded-lg bg-gray-900/50">
                                    <div className="absolute top-1/3 left-0 right-0 h-1 bg-gray-700"></div>
                                    <div className="absolute top-0 bottom-1/3 left-1/2 w-1 bg-gray-700"></div>
                                    <div className="absolute inset-0 opacity-5" style={{
                                        backgroundImage: 'repeating-linear-gradient(45deg, transparent, transparent 35px, #00ffff 35px, #00ffff 36px)',
                                    }}></div>
                                </div>

                                {/* Doors */}
                                {doors.map(door => (
                                    <div key={door.id} className="absolute" style={{ left: door.x, top: door.y }}>
                                        <div className={`w-16 h-4 rounded ${door.locked ? 'bg-red-600' : 'bg-green-600'} border-2 border-gray-800 flex items-center justify-center text-xs font-bold text-white`}>
                                            {door.locked ? '🔒' : '🚪'}
                                        </div>
                                        <div className="absolute -bottom-5 left-1/2 -translate-x-1/2 text-xs text-gray-400 whitespace-nowrap">
                                            {door.label}
                                        </div>
                                    </div>
                                ))}

                                {/* Interactive objects */}
                                {objects.map(obj => {
                                    const isNear = nearbyObject?.id === obj.id;
                                    const statusColors = {
                                        red: 'border-red-500 bg-red-900/40 shadow-red-500/50',
                                        yellow: 'border-yellow-500 bg-yellow-900/40 shadow-yellow-500/50',
                                        green: 'border-green-500 bg-green-900/40 shadow-green-500/50',
                                        gray: 'border-gray-500 bg-gray-700/40',
                                    };

                                    return (
                                        <div key={obj.id} className="absolute transition-all" style={{ left: obj.x - 25, top: obj.y - 25 }}>
                                            <div className={`w-12 h-12 rounded-lg border-2 flex items-center justify-center text-2xl cursor-pointer transition-all ${
                                                statusColors[obj.color]
                                            } ${isNear ? 'scale-125 shadow-lg animate-pulse' : ''}`}
                                                onClick={(e) => {
                                                    e.stopPropagation();
                                                    if (isNear) setInspecting(obj);
                                                }}
                                            >
                                                {obj.icon}
                                            </div>

                                            {obj.status !== 'inactive' && (
                                                <div className={`absolute -top-1 -right-1 w-3 h-3 rounded-full ${
                                                    obj.status === 'critical' ? 'bg-red-500 animate-ping' :
                                                    obj.status === 'warning' ? 'bg-yellow-500 animate-pulse' :
                                                    'bg-green-500'
                                                }`}></div>
                                            )}

                                            {isNear && (
                                                <div className="absolute -bottom-8 left-1/2 -translate-x-1/2 bg-gray-900 border-2 border-cyan-400 px-3 py-1 rounded text-xs whitespace-nowrap text-cyan-300 font-bold animate-fade-in">
                                                    {obj.label}
                                                    <div className="text-yellow-400 text-center mt-1">Click to inspect</div>
                                                </div>
                                            )}
                                        </div>
                                    );
                                })}

                                {/* Player Character */}
                                <div
                                    className="absolute transition-all duration-300 ease-out"
                                    style={{ 
                                        left: playerPos.x - 20, 
                                        top: playerPos.y - 30,
                                        transitionProperty: 'left, top'
                                    }}
                                >
                                    <div className="relative">
                                        <div className="absolute bottom-0 left-1/2 -translate-x-1/2 w-8 h-2 bg-black/40 rounded-full blur-sm"></div>
                                        <div className="relative text-4xl filter drop-shadow-lg">
                                            🧑‍🚀
                                        </div>
                                        {nearbyObject && (
                                            <div className="absolute inset-0 border-2 border-cyan-400 rounded-full animate-ping"></div>
                                        )}
                                    </div>
                                    <div className="absolute -bottom-4 left-1/2 -translate-x-1/2 bg-cyan-600 px-2 py-0.5 rounded text-xs text-white font-bold whitespace-nowrap">
                                        Engineer Kane
                                    </div>
                                </div>

                                {/* Inspection Modal */}
                                {inspecting && (
                                    <div className="absolute inset-0 bg-black/70 flex items-center justify-center z-50"
                                        onClick={() => setInspecting(null)}>
                                        <div className="bg-gray-800 border-4 border-cyan-500 rounded-lg p-6 max-w-md w-full mx-4"
                                            onClick={(e) => e.stopPropagation()}>
                                            <div className="flex items-center gap-4 mb-4">
                                                <div className="text-6xl">{inspecting.icon}</div>
                                                <div className="flex-1">
                                                    <h3 className="text-cyan-400 font-bold text-xl">{inspecting.label}</h3>
                                                    <p className={`text-sm font-bold ${
                                                        inspecting.status === 'critical' ? 'text-red-400' :
                                                        inspecting.status === 'warning' ? 'text-yellow-400' :
                                                        'text-green-400'
                                                    }`}>
                                                        STATUS: {inspecting.status.toUpperCase()}
                                                    </p>
                                                </div>
                                            </div>

                                            <div className="bg-gray-900 p-4 rounded mb-4 font-mono text-sm text-gray-300">
                                                {inspecting.type === 'panel' && (
                                                    <>
                                                        <p className="text-red-400">⚠ POWER OVERLOAD DETECTED</p>
                                                        <p className="mt-2">Circuit capacity: 100A</p>
                                                        <p>Current draw: 145A</p>
                                                        <p className="mt-2 text-yellow-400">Some circuits may be mislabeled...</p>
                                                    </>
                                                )}
                                                {inspecting.type === 'switch' && (
                                                    <>
                                                        <p>Label: "Oxygen Pump A7"</p>
                                                        <p className="mt-2 text-yellow-400">⚠ Connected system mismatch</p>
                                                        <p className="mt-2">Last accessed: Engineer Rodriguez</p>
                                                        <p className="text-xs text-gray-500">2025-11-18 08:45</p>
                                                    </>
                                                )}
                                                {inspecting.type === 'terminal' && (
                                                    <>
                                                        <p className="text-green-400">✓ SYSTEM ONLINE</p>
                                                        <p className="mt-2">Access Captain's Log</p>
                                                        <p>View System Diagnostics</p>
                                                        <p>Check Crew Assignments</p>
                                                    </>
                                                )}
                                            </div>

                                            <div className="flex gap-2">
                                                <button className="flex-1 bg-cyan-600 hover:bg-cyan-500 text-white font-bold py-2 px-4 rounded">
                                                    Investigate Further
                                                </button>
                                                <button 
                                                    onClick={() => setInspecting(null)}
                                                    className="flex-1 bg-gray-700 hover:bg-gray-600 text-gray-300 font-bold py-2 px-4 rounded"
                                                >
                                                    Close
                                                </button>
                                            </div>
                                        </div>
                                    </div>
                                )}

                                <div className="absolute bottom-4 left-1/2 -translate-x-1/2 bg-gray-900/80 border-2 border-cyan-600 px-4 py-2 rounded text-sm text-cyan-300 font-bold">
                                    Click anywhere to move 🧑‍🚀
                                </div>
                            </div>
                        </div>

                        {/* Right Panel - Context Info */}
                        <div className="w-64 bg-gray-800 border-l-2 border-cyan-600 p-3 overflow-y-auto">
                            <h3 className="text-cyan-400 font-bold text-sm mb-3">STATION INFO</h3>
                            
                            <div className="bg-gray-900 rounded p-3 mb-3 text-xs">
                                <p className="text-gray-400 mb-2">Current Location:</p>
                                <p className="text-cyan-300 font-bold">Engineering Bay - Deck 1</p>
                                <p className="text-gray-500 mt-1">Section Alpha</p>
                            </div>

                            <div className="bg-gray-900 rounded p-3 mb-3 text-xs">
                                <p className="text-gray-400 mb-2">Nearby Objects:</p>
                                {nearbyObject ? (
                                    <div className="text-yellow-300 font-bold flex items-center gap-2">
                                        <span className="text-xl">{nearbyObject.icon}</span>
                                        {nearbyObject.label}
                                    </div>
                                ) : (
                                    <p className="text-gray-500 italic">None in range</p>
                                )}
                            </div>

                            <div className="bg-gray-900 rounded p-3 text-xs">
                                <p className="text-gray-400 mb-2">Recent Log Entry:</p>
                                <div className="text-gray-300 font-mono space-y-1">
                                    <p className="text-yellow-400">[08:45]</p>
                                    <p>Engineer Rodriguez reported strange readings on power grid.</p>
                                    <p className="text-red-400 mt-2">[09:12] EMERGENCY EVAC</p>
                                </div>
                            </div>
                        </div>
                    </div>

                    {/* Bottom Tools Bar */}
                    <div className="bg-gray-800 border-t-2 border-cyan-600 p-2">
                        <div className="flex gap-2 mb-2">
                            <button
                                onClick={() => setActiveTab('log')}
                                className={`flex items-center gap-2 px-3 py-2 rounded font-bold text-xs transition-all ${
                                    activeTab === 'log' ? 'bg-cyan-600 text-white' : 'bg-gray-700 text-gray-400 hover:bg-gray-600'
                                }`}
                            >
                                <Book size={14} />
                                CAPTAIN'S LOG
                            </button>
                            <button
                                onClick={() => setActiveTab('diagnostics')}
                                className={`flex items-center gap-2 px-3 py-2 rounded font-bold text-xs transition-all ${
                                    activeTab === 'diagnostics' ? 'bg-cyan-600 text-white' : 'bg-gray-700 text-gray-400 hover:bg-gray-600'
                                }`}
                            >
                                <Activity size={14} />
                                DIAGNOSTICS
                            </button>
                            <button
                                onClick={() => setActiveTab('notebook')}
                                className={`flex items-center gap-2 px-3 py-2 rounded font-bold text-xs transition-all ${
                                    activeTab === 'notebook' ? 'bg-cyan-600 text-white' : 'bg-gray-700 text-gray-400 hover:bg-gray-600'
                                }`}
                            >
                                <FileText size={14} />
                                NOTEBOOK
                            </button>
                        </div>

                        <div className="bg-gray-900 rounded p-2 h-20 overflow-y-auto text-xs font-mono">
                            {activeTab === 'log' && (
                                <div className="space-y-1 text-gray-300">
                                    <p><span className="text-cyan-400">[08:23]</span> Rodriguez: All systems nominal.</p>
                                    <p><span className="text-yellow-400">[08:45]</span> Rodriguez: Panel 7 showing anomaly...</p>
                                    <p><span className="text-red-400">[09:12]</span> SYSTEM: CRITICAL FAILURE</p>
                                </div>
                            )}
                            {activeTab === 'diagnostics' && (
                                <div className="space-y-1 text-gray-300">
                                    <p><span className="text-red-400">ERROR:</span> Power exceeds 125% capacity</p>
                                    <p><span className="text-yellow-400">WARN:</span> O₂ fluctuating [58-65%]</p>
                                    <p><="text-orange-400">ALERT:</span> Nav coordinates drifting</p>
                                </div>
                            )}
                            {activeTab === 'notebook' && (
                                <div className="text-gray-400 italic">
                                    Your notes: Click objects to add observations...
                                </div>
                            )}
                        </div>
                    </div>
                </div>
            );
        }

        // Render the app
        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<SpaceStationGame />);
    </script>
</body>
</html>